import type { Theme } from '@nivo/core';

export const nivoTheme: Theme = {
  axis: {
    ticks: {
      text: {
        fill: 'rgb(var(--color-text-dim))',
      },
    },
  },
  grid: {
    line: {
      stroke: '#9993',
    },
  },
};
